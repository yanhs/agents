# ⚠️ Moved

This project was merged into a single repository: **yanhs/terminal**.

- Dashboard UI now lives at `terminal/web/` (served at `reimake.com/agents/`).
- Backend (status server, launch scripts) is in `terminal/`.

This repo is archived and no longer maintained. Edit the UI in
`/home/ubuntu/pr/terminal/web/index.html`.
